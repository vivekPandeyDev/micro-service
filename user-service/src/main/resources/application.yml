spring:
    application:
        name: user-service
server:
    port: 8081        

eureka:
  client:
    service-url:
      defaultZone: http://${DISCOVERY_SERVICE_NAME}:${DISCOVERY_SERVICE_PORT}/eureka/
    fetch-registry: true
    register-with-eureka: true